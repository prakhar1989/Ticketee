<span class="tag" id="tag-<%= tag.name.parameterize %>">
  <%= link_to "x", remove_ticket_tag_path(@ticket, tag), :remote => true, :method => :delete, :id => "delete-#{tag.name.parameterize}" %>
</span>
